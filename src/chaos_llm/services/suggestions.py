SUGGESTIONS = {
    "S0": [
        "SELECT", "FILTER", "GROUP_BY", "ORDER_BY",
        "SUM(", "MEAN(", "VAR(", "DIFF(", "SIMPLIFY("
    ],
    "S1": [
        "WHERE", "LIMIT", "OFFSET", "JOIN"
    ],
}
